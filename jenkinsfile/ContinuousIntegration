pipeline {
    agent any
    stages {
        stage('Compile and Test') {
            steps {
                echo 'Cleaning Workspace'
                sh 'chmod +x ./gradlew'
                sh './gradlew clean'

                echo 'Compile and Test'
                sh './gradlew test'
            }
        }

    }

}